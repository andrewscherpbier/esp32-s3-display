# Alarm clock settings, layered on ../../common/sdkconfig.defaults

# TLS (weather over HTTPS) allocates its buffers from PSRAM, keeping internal RAM free
CONFIG_MBEDTLS_EXTERNAL_MEM_ALLOC=y

CONFIG_LV_FONT_MONTSERRAT_24=y
